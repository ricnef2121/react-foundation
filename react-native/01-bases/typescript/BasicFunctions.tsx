const addTwonumbers = (a: number, b: number): string => `${a + b}`;

export const BasicFunctions = () => {


    return (
        <>
            <div>BasicFunctions</div>
            <p>
                {addTwonumbers(2, 9)}
            </p>
        </>
    )
}
